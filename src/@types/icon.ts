import { CSSProperties } from 'react';

export type IconProps = {
  width?: number | 'auto';
  height?: number | 'auto';
  color?: string;
  unit?: string;
  style?: CSSProperties;
  ratio?: number;
};
